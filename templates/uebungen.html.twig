<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="css/styles.css">
  <title>{{titel}}</title>
</head>

<body>
  <div class="site-wrapper">
  {% include (incHeader) with {'username': username, 'path' : './'} %}
    <main>
    {% include (incNav) with {'pos': pos, 'path': 'uebungen/' } %}
      <div>
      </div>
    </main>
    {{include (incFooter)}}
  </div>
</body>

</html>